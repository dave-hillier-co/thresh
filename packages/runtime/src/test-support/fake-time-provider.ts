// Re-exported from core (where the clock now lives) so existing
// `@thresh/runtime/test-support/fake-time-provider` imports keep working.
export * from "@thresh/core/test-support/fake-time-provider";
